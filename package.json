{
  "scripts": {
    "tw": "tailwindcss -i styles.css -o dist/styles.css --watch",
    "server": "reload -b -p 8081",
    "start": "concurrently -gk -n tw,s --hide s -p none \"npm run tw\" \"npm run server\""
  },
  "devDependencies": {
    "reload": "^3.2.0",
    "tailwindcss": "^3.2.0"
  }
}